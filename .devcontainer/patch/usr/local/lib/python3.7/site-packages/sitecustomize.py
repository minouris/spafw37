try:
    import typing
    from typing_extensions import Literal, TypedDict
except Exception:
    pass
else:
    for _name in ("Literal", "TypedDict"):
        if not hasattr(typing, _name):
            setattr(typing, _name, locals()[_name])